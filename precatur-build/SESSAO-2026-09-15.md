# Sessão Precatur × Twenty CRM — 2026-09-15 (handoff p/ continuação)

> Continuação da sessão de 2026-07-24 (`SESSAO-2026-07-24.md`). Fonte-de-verdade do modelo: `~/twenty/PRECATUR_MODEL.md` (aqui: `../PRECATUR_MODEL.md`).

## Objetivo desta sessão
Criar no Twenty os campos novos dos 4 pipelines, conforme **lista autoritativa do cliente** (colada na conversa; ~69 itens com `categoria_twenty` ∈ {Negociações, Análises Jurídicas, Processos Administrativos, Pagamentos} e 5 itens marcados `precisa_revisar`). Nenhum campo foi mapeado para o objeto central Precatório (ele já tem os campos do modelo).

## Estado do ambiente (montado do zero nesta sessão)
- **Repo**: `/Users/pietro_medeiros/Downloads/twenty-precatur/twenty` — fork, branch `feat/precatur-gerador-proposta`, source na versão **2.25.0-dev** (commits: modelo de dados + gerador de proposta + CI de build de imagem). ~~A cópia antiga `~/twenty` da sessão anterior não existe mais aqui; o `.env` antigo (com ENCRYPTION_KEY/APP_SECRET de julho) se perdeu.~~ **Errado — ver RESULTADO**: `~/twenty` existe e guardava o `.env` de julho; os segredos foram recuperados de lá.
- **Banco**: volume Docker `twenty_db-data` **intacto** (dados de 24/07 preservados). Senha do postgres: `postgres` (verificada). 1 workspace: id `d77c37a8-ea55-421e-b7d5-e49dc028aa27`, nome `precatur-sandbox`, schema `workspace_cr9e3ud3xqww3w5zrpydlcap3`. Os 5 objetos (precatorio, negociacao, analiseJuridica, processoAdministrativo, pagamento) e 96 campos custom existem (conferido em `core.fieldMetadata` ⋈ `core.objectMetadata`).
- **Conflito de porta**: `webshield-postgres` (outro projeto Docker, ativo) ocupa a 5432. O override `packages/twenty-docker/docker-compose.override.yml` foi alterado para **`5433:5432`** (host:container). Anotação no próprio arquivo.
- **`.env` Docker recriado** em `packages/twenty-docker/.env`: TAG=latest, PG postgres/postgres, SERVER_URL=http://localhost:3000, STORAGE_TYPE=local, **ENCRYPTION_KEY e APP_SECRET NOVOS** (openssl). Consequência: segredos criptografados de julho (ex.: API keys antigas em `core.apiKey`, se houver) não descriptografam — usar SEMPRE key nova. Logins por senha são bcrypt, não afetados.
- **Imagens Docker são beco sem saída para este banco**: o schema foi migrado em 24/07 pelo **dev mode a partir deste source** (2.25.0-dev). Tanto a imagem cached antiga (sequência até 2.24.0) quanto a nova `latest` puxada do Hub divergem do schema (erros reais: `permissionFlag.roleId` inexistente; enum `core.pageLayoutWidget_type_enum` sem valor `MESSAGE_CAMPAIGN_BODY`). Não insistir no Docker mode até existir imagem buildada deste fork (há workflow de CI para isso; pendência antiga).
- **Decisão: server em dev mode sobre o mesmo banco Docker** (dados preservados; único código garantido a casar com o schema).
- `.env` dev criado em `packages/twenty-server/.env`: NODE_ENV=development, `PG_DATABASE_URL=postgres://postgres:postgres@localhost:5433/default`, REDIS_URL=redis://localhost:6379, APP_SECRET/ENCRYPTION_KEY copiados do .env Docker, SIGN_IN_PREFILLED=true, IS_WORKSPACE_CREATION_LIMITED_TO_SERVER_ADMINS=false, FRONTEND_URL=:3001, SERVER_URL=:3000, STORAGE_TYPE=local.
- Containers `twenty-server`/`twenty-worker` **parados** (`docker compose stop server worker`); `db` e `redis` no ar.
- `yarn install` OK (Node v24.18.0 via nvm, corepack yarn 4). `nx start:ci` falhou na 1ª tentativa: falta `twenty-shared/dist` → build `corepack yarn nx build twenty-shared` foi **cancelado a pedido do cliente**; precisa ser reexecutado (passo 1 abaixo).

## ✅ RESULTADO (execução concluída em 15/09, noite)
- **59 campos criados** nos 4 objetos (58 na 1ª passada + `analiseJuridica.natureza` na 2ª). 0 falhas. Script idempotente confirmado: 3ª execução = 59 pulados, 0 criados.
- Contagem final de campos por objeto (inclui os de sistema): negociacao 48, analiseJuridica 53, processoAdministrativo 35, pagamento 29, precatorio 30 (intocado).
- **Segredos de julho RECUPERADOS**: a cópia antiga `~/twenty` ainda existe e tinha `packages/twenty-docker/.env` com o `ENCRYPTION_KEY`/`APP_SECRET` originais. Sem eles o comando de API key falhava (`No encryption key matches keyId '9c4585b0'` → `No active signing key available`). O `.env` dev agora usa ENCRYPTION_KEY/APP_SECRET **de julho** + `FALLBACK_ENCRYPTION_KEY` = a chave nova gerada nesta sessão. Backups: `packages/twenty-{server,docker}/.env.bak-newkeys`.
- **Builds extras necessários** além do twenty-shared: `twenty-client-sdk` e `twenty-emails` (o `start:ci` falha com MODULE_NOT_FOUND sem eles).
- **API key** gerada via `nx run twenty-server:command-no-deps -- workspace:generate-api-key` (o `npx tsx src/command/command.ts` quebra: macro do Lingui, `import_macro.msg is not a function`).
- **2 correções no script** (`criar-campos-pipelines.mjs`), necessárias nesta versão do metadata API:
  1. `object(input:{nameSingular})` não existe mais → passou a usar `objects(paging:{first:200})` e filtrar em memória.
  2. `options`/`settings` são scalars JSON e devem ir como **objeto via variável GraphQL**; como string JSON o server responde `options?.map is not a function`. Era isso que derrubava o SELECT `natureza` e fazia os 4 NUMBER caírem no fallback "mínimo" (sem settings).
- Os 4 NUMBER tiveram `settings` aplicados depois por `updateOneField`: `percentualDesembolso` e `honorariosContratuaisPercentual` = float/2 casas; `anoOrcamentario` e `previsaoPagamentoAno` = int.
- ⚠️ **Efeito colateral corrigido**: o update por nome pegou também `precatorio.anoOrcamentario` (campo de julho), que recebeu `{"dataType":"int"}` e foi **revertido para `null`** (estado original) no mesmo minuto. Nenhuma outra alteração fora do escopo.

## Confirmações do dono (15/09, ao final da execução)
- **Duplicados**: aceita os equivalentes de julho — os 5 itens da lista nova NÃO viram campo separado.
- **Itens ⚠️**: todas as interpretações confirmadas como estão (`resposta` TEXT; 3 BOOLEANs de aprovação; autorizações como TEXT; Contrato Honorários como anexo).
- **Ambiente**: server dev derrubado ao final; Postgres/Redis (Docker) seguem no ar com os dados.
- `PRECATUR_MODEL.md` atualizado com a lista dos 59 campos por setor.

## Passos pendentes (ordem)
1. Garantir `corepack yarn nx build twenty-shared` ok (from repo root, com nvm use 24).
2. Subir server dev em background: `corepack yarn nx start:ci twenty-server` e aguardar `curl localhost:3000/healthz` = 200 (SWC ~1min + boot Nest + migrations; ~2-3min total).
3. **Gerar API key** (comando oficial, só existe em NODE_ENV=development — por isso falhou nos containers):
   ```bash
   cd packages/twenty-server
   set -a; source .env; set +a
   npx tsx src/command/command.ts workspace:generate-api-key -w d77c37a8-ea55-421e-b7d5-e49dc028aa27 -n "Precatur campos pipelines"
   # saída: TOKEN:<jwt>  (chaves de assinatura ficam no banco, então o server dev valida)
   ```
   Alternativa: criar a key na UI (localhost:3000 → Settings → APIs & Webhooks), mas o ENCRYPTION_KEY novo pode invalidar o que existir — key nova de qualquer forma.
4. **Criar os campos**: `TWENTY_API_KEY=<jwt> node precatur-build/criar-campos-pipelines.mjs` (idempotente; pula existentes por nome; relatório criados/pulados/falhos).
5. Verificar: reexecutar o script (tudo pulado) e/ou conferir na UI os 4 objetos.
6. Reportar ao cliente: campos criados, os **pulados por já existirem** (ver abaixo) e os **⚠️ marcados p/ revisão** — pedir confirmação.
7. Atualizar `PRECATUR_MODEL.md` (seção de campos por setor) com os novos campos. Manter este arquivo como registro da sessão.
8. Desligar com carinho: `kill` do processo dev (ou deixar rodando conforme pedido do cliente). Para voltar ao Docker mode puro depois: `docker compose start server worker` — mas lembrar que a imagem não casa com o schema até haver build do fork.

## O que o script `criar-campos-pipelines.mjs` faz
- 57 campos novos, spec embutida com tipos, ícones omitidos propositalmente (mínimo de atrito), `isNullable: true`, retentativa sem `description/settings` se a 1ª tentativa falhar.
- **Pulados de propósito** (já existem, equivalentes de julho): `Origem do Lead`→`canalOrigem` (Negociação); `CNPJ / CPF` e `CPF`→`cpfCnpjCedente` (Negociação); `Parecer Jurídico`→`parecerJuridico` (Análise); `Comprovante de Pagamento`→`comprovante` (Pagamento). Confirmar com o cliente se aceita o equivalente ou quer campo separado.
- **Interpretações dos itens `precisa_revisar: true`** (avisar o cliente):
  - `3. "Resposta"` → TEXT `resposta` (Negociação) — nome literal do formulário.
  - `4. Aprovações (Comercial, Jurídico, Administrativo)` → 3 BOOLEANs no Processo Adm: `aprovacaoComercial/Juridica/Administrativa`.
  - `Autorização - Responsável Adm/Financeiro` e `Autorização - Responsável Jurídico` → TEXT (podem virar relação → workspaceMember depois).
  - `Contrato Honorários` → TEXT de anexo (Análise).
- **Padrão de anexos**: campos de documento viram TEXT com descrição "suba o arquivo na aba Attachments" (mesmo padrão decidido em julho para `comprovante` — o tipo FILES não é criável via API em custom objects).
- `Telefone` → TEXT (PHONES não arriscado).
- Tipos de valor: moedas→CURRENCY; `%`→NUMBER float 2 casas; anos→NUMBER int; listas curtas→SELECT (só `natureza`: Alimentar/Comum); textos longos→RICH_TEXT (`consideracoes`, `observacoesPreAnalise`, `pendenciasProvidenciar`); flags→BOOLEAN (`aceite`, `confirmacaoDiretorComercial`, aprovações).

## Observações
- Máquina do cliente: não sobrecarregar — dev server é 1 processo Node (~1-1,5GB), derrubar ao final se pedido. Já rodam webshield (ES inclusive) + outros containers.
- `docker compose down` no projeto twenty é seguro (dados em volumes), mas o override agora usa 5433 — não reverter para 5432 enquanto webshield estiver ativo.
- Pendências antigas (24/07) continuam válidas: atribuir os 4 roles aos membros; estratégia de deploy (imagem custom do fork via CI já existe).
